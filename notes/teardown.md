# Resource Teardown

After validating the Auto Scaling behavior, all AWS resources were deleted to:

- Avoid unnecessary AWS charges
- Keep the AWS account clean

Screenshots are retained as evidence of successful implementation.
